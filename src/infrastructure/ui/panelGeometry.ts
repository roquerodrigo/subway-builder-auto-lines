// The window the game opens for the panel: the size it starts at, and the floor a
// player can shrink it to. PanelViewport assumes the same default size when a
// stored geometry carries only a position.
export const DEFAULT_PANEL_WIDTH = 500
export const DEFAULT_PANEL_HEIGHT = 700
export const MIN_PANEL_WIDTH = 320
export const MIN_PANEL_HEIGHT = 380
